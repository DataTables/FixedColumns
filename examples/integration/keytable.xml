<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html-wide" table-class="stripe row-border order-column nowrap" order="4">

<css lib="datatables fixedcolumns keytable"/>
<js lib="jquery datatables fixedcolumns keytable">
<![CDATA[
$(document).ready(function() {
	$('#example').DataTable( {
		scrollY:        300,
		scrollX:        true,
		scrollCollapse: true,
		paging:         false,
		fixedColumns:   true,
		keys:           true
	} );
} );
]]>
</js>

<title lib="FixedColumns">KeyTable</title>

<info><![CDATA[

This example shows the [KeyTable extension](//datatables.net/extensions/keytable) for DataTables being used with FixedColumns. This example doesn't actually do anything functionally - normally you would listen for events from the key event listener to perform actions such as editing, or integrate it with Editor.

]]></info>

</dt-example>
